# Database configuration
# For local development the backend falls back to SQLite automatically.
# Set this to a real PostgreSQL URL when the DB team provides credentials.
DATABASE_URL=postgresql://username:password@localhost:5432/dbname

# API Keys
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Flask configuration
FLASK_ENV=development
PORT=5000
